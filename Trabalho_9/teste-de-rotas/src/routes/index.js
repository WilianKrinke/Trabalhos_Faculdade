import React from 'react';
import { BrowserRouter, Routes, Route } from "react-router-dom";

const index = () => {
    return (
        <BrowserRouter>
            <Routes>

            </Routes>            
        </BrowserRouter>
    );
}

export default index;
